# CPU-only PyTorch wheels. This service only runs on CPU (see main.py) — installing
# the default CUDA build here causes intermittent DLL init failures (WinError 1114)
# on machines with an NVIDIA GPU/driver present but not matched to the CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pydantic==2.8.0

# Image preprocessing
opencv-python==4.10.0.84

# OCR
easyocr==1.7.1
torch==2.12.1+cpu
torchvision==0.27.1+cpu

# Document processing
PyMuPDF==1.24.5
python-docx==1.1.2
Pillow==10.4.0

# Ollama client
ollama==0.3.3

# Image moderation
nudenet==3.4.2

# Utilities
python-dotenv==1.0.1
aiofiles==24.1.0
numpy>=1.24.0
